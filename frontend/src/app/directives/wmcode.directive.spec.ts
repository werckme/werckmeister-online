import { WmcodeDirective } from './wmcode.directive';

describe('WmcodeDirective', () => {
  it('should create an instance', () => {
    const directive = new WmcodeDirective();
    expect(directive).toBeTruthy();
  });
});
