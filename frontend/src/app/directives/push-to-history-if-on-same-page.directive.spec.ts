import { PushToHistoryIfOnSamePageDirective } from './push-to-history-if-on-same-page.directive';

describe('PushToHistoryIfOnSamePageDirective', () => {
  it('should create an instance', () => {
    const directive = new PushToHistoryIfOnSamePageDirective();
    expect(directive).toBeTruthy();
  });
});
