import { EmbeddedLinkDirective } from './embedded-link.directive';

describe('EmbeddedLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new EmbeddedLinkDirective();
    expect(directive).toBeTruthy();
  });
});
