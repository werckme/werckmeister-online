import { DragableDirective } from './dragable.directive';

describe('DragableDirective', () => {
  it('should create an instance', () => {
    const directive = new DragableDirective();
    expect(directive).toBeTruthy();
  });
});
