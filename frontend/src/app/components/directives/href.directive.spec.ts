import { HrefDirective } from './href.directive';

describe('HrefDirective', () => {
  it('should create an instance', () => {
    const directive = new HrefDirective();
    expect(directive).toBeTruthy();
  });
});
